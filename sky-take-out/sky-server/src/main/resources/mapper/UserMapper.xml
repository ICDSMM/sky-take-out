<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.UserMapper">

    <!--    插入微信新用户数据-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user (openid, name, phone, sex, id_number, avatar, create_time) VALUES (#{openid},#{name},#{phone},#{sex},#{idNumber},#{avatar},#{createTime})</insert>

    <!--    根据openid查询微信用户-->
    <select id="getByOpenId" resultType="com.sky.entity.User">
        select * from user where openid = #{openid}
    </select>

    <!--    根据用户id查询用户信息-->
    <select id="getById" resultType="com.sky.entity.User">
        select * from user where id = #{userId}
    </select>
    <select id="countByMap" resultType="java.lang.Integer"></select>

</mapper>